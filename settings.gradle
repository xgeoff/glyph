pluginManagement {
    includeBuild("../grimoire")
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'glyph'

include 'plugins:glyph-gradle-plugin'
include ':glyph-core'
project(':glyph-core').projectDir = file('core')
